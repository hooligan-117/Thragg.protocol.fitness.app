/**
 * THRAGG PROTOCOL — Supabase Configuration
 * =========================================
 * Replace the values below with your actual Supabase project credentials.
 * Then add this file to your GitHub repo alongside index.html.
 *
 * HOW TO USE:
 * 1. Go to supabase.com → your project → Settings → API
 * 2. Copy "Project URL" and paste it as SUPABASE_URL below
 * 3. Copy "anon / public" key and paste it as SUPABASE_KEY below
 * 4. Add this file to your GitHub repo — it will be served alongside index.html
 *
 * SECURITY NOTE:
 * The anon key is safe to expose in a public repo. It only has the
 * permissions you define in Supabase Row Level Security (RLS) policies.
 * The starter SQL schema in the Settings panel grants full access,
 * which is fine for a personal single-user app.
 *
 * If you'd rather not commit credentials to GitHub, use the
 * Settings → Supabase Connection form in the app to enter them manually.
 * They will be saved to localStorage in your browser.
 */

const SUPABASE_URL = 'https://YOUR_PROJECT_ID.supabase.co';
const SUPABASE_KEY = 'YOUR_ANON_KEY_HERE';
